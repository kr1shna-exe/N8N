{
  "buildCommand": "turbo run build --filter=vite_react_shadcn_ts",
  "installCommand": "bun install",
  "framework": null,
  "outputDirectory": "apps/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
