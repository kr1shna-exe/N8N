{
  "buildCommand": "rm -rf dist node_modules/.vite && bun run build",
  "installCommand": "bun install --no-save",
  "framework": null,
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
